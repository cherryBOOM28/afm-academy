
import './style.scss';

function Centered({ children }) {
    return (
        <div
            className="centered"
        >
            {children}
        </div>
    );
}

export default Centered;